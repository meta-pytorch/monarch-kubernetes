apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monarch-operator.fullname" . }}-test-connection"
  labels:
    {{- include "monarch-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monarch-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
