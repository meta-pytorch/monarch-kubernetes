apiVersion: v1
kind: Service
metadata:
  name: {{ include "monarch-operator.fullname" . }}
  labels:
    {{- include "monarch-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monarch-operator.selectorLabels" . | nindent 4 }}
