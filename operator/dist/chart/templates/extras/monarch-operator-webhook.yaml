apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator.resourceName" (dict "suffix" "monarch-operator-webhook" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    control-plane: controller-manager
